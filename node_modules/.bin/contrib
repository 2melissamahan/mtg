#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../contrib/bin/contrib" "$@"
  ret=$?
else 
  node  "$basedir/../contrib/bin/contrib" "$@"
  ret=$?
fi
exit $ret
